import { Time } from "@angular/common"

export interface Orders {
    name_crystal: string;
    name_client: string;
    quantity: number;
    id_order: number;
    id_stock: number;
    id_client: number;
    time: number;
  }
